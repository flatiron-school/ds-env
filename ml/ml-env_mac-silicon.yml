name: ml-env
channels:
  - defaults
  - conda-forge
dependencies:
  - python==3.12
  - numpy
  - pandas
  - matplotlib
  - seaborn
  - scipy
  - scikit-learn
  - statsmodels
  - python-graphviz
  - ipykernel
  - jupyter
  - pip
  - pip:
      - shap
      - lime
      - plotly
      - faraway
  
  
# conda env create -f ml-env_mac-silicon.yml
# python -m ipykernel install --user --name ml-env --display-name "Python (ml-env)"