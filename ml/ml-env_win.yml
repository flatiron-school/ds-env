name: ml-env
channels:
  - defaults
  - conda-forge
dependencies:
  - python==3.9
  - numpy
  - pandas
  - matplotlib
  - seaborn
  - scipy
  - scikit-learn
  - statsmodels
  - python-graphviz
  - jupyter
  - ipykernel
  - pip
  - pip:
      - shap
      - lime
      - plotly
      - faraway
      

# conda env create -f ml-env_win.yml
# python -m ipykernel install --user --name ml-env --display-name "Python (ml-env)" 